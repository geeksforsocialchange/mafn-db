wb = xlsx_package.workbook
wb.add_worksheet(name: "Events Feedback") do |sheet|
  sheet.add_row [
    "Event External ID",
    "How Did You Find Out About Today",
    "Other How you found out",
    "Have previously attended an event or activity",
    "Why did you attend today",
    "Would you recommend future events/activities to others?",
    "To what extent do you feel that you live in an age-friendly neighbourhood?",
    "Could you tell us why you think this?",
    "I can influence decisions affecting my local area",
    "People can change things in my local area if they work together",
    "Could you tell us why you think this?",
    "Do you have any comments or feedback about the day for us?",
    # TODO: Remove when handing over
    "Member ID"
  ]
  @events.each do |event|
    event.responders.each do |member|
      sheet.add_row [
        # "Event External ID"
        event.id,
        # "How Did You Find Out About Today"
        last_response(member, "How did you find out about today?", event.entity_id),
        # "Other How you found out"
        last_response(member, "How did you find out about today? (other reason)", event.entity_id),
        # "Have previously attended an event or activity"
        Member.find(member).events.count,
        # "Why did you attend today"
        last_response(member, "Why did you attend today?", event.entity_id),
        # "Would you recommend future events/activities to others?"
        "TODO",
        # "To what extent do you feel that you live in an age-friendly neighbourhood?"
        "TODO",
        # "Could you tell us why you think this?"
        "TODO",
        # "I can influence decisions affecting my local area"
        last_response(member, "I can influence decisions affecting my local area", event.entity_id),
        # "People can change things in my local area if they work together"
        last_response(member, "People can change things in my local area if they work together", event.entity_id),
        # "Could you tell us why you think this?"
        "TODO",
        # "Do you have any comments or feedback about the day for us?"
        "TODO",
        Member.find(member).id
      ]
    end
  end
end
