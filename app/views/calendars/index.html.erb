<p id="notice"><%= notice %></p>

<h1>Listing Calendars</h1>

<table class="table">
  <thead>
    <tr>
      <th>Project</th>
      <th>Is funded default</th>
      <th>Region default</th>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @calendars.each do |calendar| %>
      <tr>
        <td><%= calendar.project %></td>
        <td><%= calendar.is_funded_default %></td>
        <td><%= calendar.region_default %></td>
        <td><%= calendar.url %></td>
        <td><%= link_to 'Show', calendar %></td>
        <td><%= link_to 'Edit', edit_calendar_path(calendar) %></td>
        <td><%= link_to 'Destroy', calendar, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Calendar', new_calendar_path %>
